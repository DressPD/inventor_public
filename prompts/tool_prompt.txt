Python sandbox for autonomous inventory analysis.

Your code is the reproducible audit artifact. Write clean, commented Python.

MANDATORY FIRST ACTION: load the demand data from the resource manager URL:
    df = pd.read_csv(data_url, sep=';')
    print(f"Loaded {len(df)} rows")
The variable `data_url` is a self-contained presigned download URL for the uploaded CSV.
It requires NO authentication. Use it directly with pd.read_csv.
Do NOT hardcode or fabricate any data values — read them from df.

THEN: save the fully computed JSON before any plots:
    json.dump(result, open('inventory_optimization_output.json', 'w'), indent=2)

Full stack: pandas, numpy, scipy, matplotlib, math, json, io, requests.
Persistent workspace. Multi-step execution. Debug errors and retry.

Save plots:  plt.savefig('name.png', dpi=150, bbox_inches='tight'); plt.close()
             Reference: <png>name.png</png>

Save tables: df.to_markdown('table.md', index=False)
             Reference: <md>table.md</md>

All saved files are auto-attached to the response.
