# flagging_method

LLM-facing material triage and review queue layer.

Use this folder for:

- special-material flag queues
- decision-card tests
- planner-facing review prompts
- reviewed flag queues (`review_material_flags.py`)
- deterministic and traditional ML flagging baselines (`baseline_flagging_models.py`)
- targeted optimization impact evaluation (`evaluate_targeted_improvement.py`)
